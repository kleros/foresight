{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "inputs": ["config.localhost.yaml", "config.gnosis.yaml", "schema.graphql"],
      "outputs": [".envio/types.d.ts"],
      "passThroughEnv": ["ENVIO_*"]
    },
    "check-types": {
      "dependsOn": ["codegen", "@foresight/session-metadata#check-types"]
    },
    "test": {
      "dependsOn": ["codegen", "@foresight/session-metadata#check-types"]
    }
  }
}
